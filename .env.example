# libSQL connection URL. If omitted, the app defaults to a local file DB.
# Local development (creates local.db in the project root):
DATABASE_URL=file:./local.db

# Production (Turso): replace with your database URL and token from the Turso dashboard.
# DATABASE_URL=libsql://your-db-name-your-org.turso.io?authToken=...
